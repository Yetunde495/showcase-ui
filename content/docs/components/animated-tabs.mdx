---
title: Animated Tabs
description: A collection showcase with vertical navigation, progress indicators, and synchronized content updates.
---

import { Tab, Tabs } from "fumadocs-ui/components/tabs";
import { Step, Steps } from "fumadocs-ui/components/steps";
import { SourceCode } from "@/components/source-code";
import { ComponentPreview } from "@/components/component-preview";
import AnimatedTabs from "@/registry/default/example/animated-tabs";
import {AnimatedTabsWithArrowDemo} from "@/registry/default/demo/animated-tabs-demo";


<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <ComponentPreview name="animated-tabs" full />
  </Tab>
  <Tab value="Code">
    <SourceCode name="animated-tabs" />
  </Tab>
</Tabs>

## Animated Tabs (With Arrow Navigation)

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <ComponentPreview name="" full>
    <AnimatedTabsWithArrowDemo />
    </ComponentPreview>
  </Tab>

  <Tab value="Code">
    <SourceCode name="animated-tabs" />
  </Tab>
</Tabs>

This variant enables optional arrow controls for users who prefer explicit navigation buttons, while still supporting swipe gestures.

## Installation

<Tabs items={['CLI', 'Manual']}>
  <Tab value="CLI">
    ```bash
    npx shadcn@latest add "https://uselayouts.com/r/animated-tabs.json"
    ```
  </Tab>
  <Tab value="Manual">
    <Steps>
      <Step>
        ### Install dependencies

        ```bash
        npm install motion @hugeicons/react @hugeicons/core-free-icons
        ```
      </Step>
      <Step>
        ### Copy the code

        Copy the code from the **Code** tab above into `components/vertical-tabs.tsx`.
      </Step>
      <Step>
        ### Update imports

        Update the imports to match your project structure.
      </Step>
    </Steps>

  </Tab>
</Tabs>

## Usage

### Customizing Content

Modify the `SERVICES` array at the top of the file to add your own sections:

```tsx
// Change Here
const SERVICES = [
  {
    id: "01",
    title: "Web Design",
    description: "Creating beautiful digital experiences.",
    image: "https://images.unsplash.com...",
  },
  // ...
];
```

```tsx
import AnimatedTabs from "@/components/animated-tabs";

export default function Page() {
  return <AnimatedTabs />;
}
```

## Features

- **Swipe Navigation**: Natural horizontal drag gestures on touch devices.
- **Optional Arrow Controls**: Enable arrow buttons for explicit navigation.
- **Overflow Awareness**: Edge blur indicates hidden items.
- **Directional Transitions**: Content animates based on navigation direction.
- **No Layout Shift**: Fixed-width tabs with adaptive expansion.
- **Auto-cycle**: Automatically rotates through tabs (pauses on hover).

## Props

| Prop         | Type      | Default | Description                      |
| ------------ | --------- | ------- | -------------------------------- |
| `tabs`       | `Tab[]`   | —       | Tab configuration data           |
| `showArrows` | `boolean` | `false` | Enables arrow navigation buttons |
